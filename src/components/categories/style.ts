import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        height: 52,
        maxHeight: 52
    },

    content: {
        gap: 16, // espaçamento entre itens da lista
        paddingHorizontal: 24, // espaçamento das bordas laterais do celular
    }
})